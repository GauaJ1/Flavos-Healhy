# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

Ideias

# Android build artifacts
android/app/build/
android/.gradle/

# APK files — never commit builds to the repository
# Distribute via GitHub Releases only
*.apk

# Android signing keystore — NEVER commit to git
*.jks
*.keystore
release.properties

# Environment variables — NEVER commit these
.env
.env.*
!.env.example

# Agent workspace — internal tooling, never public
.agents/

# migrations/ é intencionalmente versionada — schema SQL sem segredos
.vercel
